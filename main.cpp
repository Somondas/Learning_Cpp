#include <iostream>

int main(){
  int x = 21;
  int y = 2;
  int z = x % y;
  std::cout << z;
  return 0;
}

// Important: In math or in any programming language, the '*' and '/' have higher prioriety than '+' and '-'

/*
*  Important: When working with float datatype, alaways add a F(F can be uppercase or lowercase) in the end of the number
* Example: float sample = 32.1F;
*/
